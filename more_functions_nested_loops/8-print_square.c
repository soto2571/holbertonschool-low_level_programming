#include "main.h"
/**
 *print_square - Start of the program
 *@size:Print a square
 *Return:0
 */
void print_square(int size)
{
char hash = '#';
int x;
int y;

if (size > 0)
{
	while (a < size)
	{
		while (b < size)
		{
		_putchar(hash);
		b++;
		}
	b = 0;
	a++;
	_putchar('\n');
	}
}
else
{
	_putchar('\n');
}
}
