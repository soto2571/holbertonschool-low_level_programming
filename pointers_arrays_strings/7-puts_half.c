#include "main.h"
/**
 *puts_half - Start of the program
 *@str: Prints half of the string
 *
 */
void puts_half(char *str)
{
int len = 0;
int i = 0;

while (str[len] != '\0')
{
	len++
}
while (i < len)
{
	_putchar(str[i]);
	i / 2;
}
}
