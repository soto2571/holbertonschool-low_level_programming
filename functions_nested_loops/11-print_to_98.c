#include "main.h"
#include <stdio.h>
/**
 *print_to_98 - Start of the prgoram
 *@n:number
 *Return:0
 */
void print_to_98(int n)
{
int x;

if (x > 0)
{
for (x > 0; x <= 98, x++)
{
	_putchar(',');
	_putchar(' ');
}
}
if (x < 0)
{
for (x < n; x <= 98; x--)
{
	_putchar(',');
	_putchar(' ');
}
	_putchar(x);
}
}
