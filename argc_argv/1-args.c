#include "main.h"
#include <stdio.h>
/**
 *main - start of the program
 *@argc:Count the lines
 *@argv:Count the strings
 *Return:0
 */
int main(int argc, char *argv[])
	(void)argv;
	printf("%d\n", argc - 1);
	return (0);
}
